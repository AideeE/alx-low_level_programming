#include "main.h"

/**
 * mul - multiplies two integers
 * @a: a - The first number to multiply
 * @b: b - The second number to multiply with first number
 * Return: The result of the multiplication between a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
